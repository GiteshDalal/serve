/*
 * @author Serve Engine
 *
 * Please don't make any changes in this file because it is generated by Serve Engine Plugin
 * and thus, changes will be overwritten. To make any changes to this file, you can edit 
 * "src/main/resources/models.yml" or you can edit "src/main/resources/templates/resource.vm".
 * Note: Changes made in resource.vm file will reflect in all generated *Resource.java files.
 */

package $config.resourcePackage;

import lombok.*;
import java.util.*;
import ${config.resourcePackage}.*;
import org.springframework.hateoas.ResourceSupport;
import com.fasterxml.jackson.annotation.JsonIgnore;

@Data
@EqualsAndHashCode(callSuper = true)
@SuppressWarnings(value = { "unused" })
public class ${model.name}Resource extends#if( $model.parent ) ${model.parent}Resource #else ResourceSupport #end{

#foreach($att in $model.attributes)
#if(!$att.includeInResource)
	@JsonIgnore
#end
#if( $StringUtils.isNotBlank($att.related) )
#if( $StringUtils.isNotBlank($att.type) )
	private ${att.type}<${att.related}Resource> $att.name;
#else
	private ${att.related}Resource $att.name;
#end
#else
	private $att.type $att.name;
#end
#end

}