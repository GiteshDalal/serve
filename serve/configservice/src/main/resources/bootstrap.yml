---
spring:
  application:
    name: config-service
    
  cloud:
    config:
      server:
        git:
          uri: ${HOME}/work/study/spring/configurations-repository
          
      discovery:
        enabled: true