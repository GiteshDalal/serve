/*
 * @author Serve Engine
 *
 * Please don't make any changes in this file because it is generated by Serve Engine Plugin
 * and thus, changes will be overwritten. To make any changes to this file, you can edit 
 * "src/main/resources/templates/repository.vm" file and changes will reflect in all
 * generated *Repository.java files.
 * For creating custom Repository you can extend this repository interface to another one.
 */

package $config.repositoryPackage;

import java.util.*;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.querydsl.QuerydslPredicateExecutor;
import org.springframework.data.querydsl.binding.QuerydslBinderCustomizer;
import org.springframework.data.querydsl.binding.QuerydslBindings;
import org.springframework.stereotype.Repository;
import com.giteshdalal.productservice.model.generated.*;
import com.querydsl.core.types.dsl.StringPath;

@Repository
@SuppressWarnings(value = { "unused" })
public interface ${model.name}Repository extends JpaRepository<${model.name}Model, ${model.idType}>, QuerydslPredicateExecutor<${model.name}Model>,
		QuerydslBinderCustomizer<Q${model.name}Model> {

	@Override
	default void customize(QuerydslBindings bindings, QProductModel model) {
		bindings.bind(String.class).first((StringPath path, String value) -> path.containsIgnoreCase(value));
	}

}