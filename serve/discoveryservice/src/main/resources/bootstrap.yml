---
spring:
  application:
    name: discovery-service
  profiles:
    active: dev,local
  main:
    banner-mode: "off"

  cloud:
    config:
      enabled: false

server:
  port: 8761

keycloak:
  enabled: false